frontend:
  phases:
    preBuild:
      commands:
        - nvm install 7.6.0
        - nvm use 7.6.0
        - node -v  # (Optional) to log the current version
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
